class interCookie:
    with open('cookies.txt', 'r') as fileContent:
        contents = fileContent.read()
        #print(contents)
        #this works thus far
        
class setUp
        
interCookie()
